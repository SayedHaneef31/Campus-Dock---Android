<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="?attr/colorSurfaceVariant"
    android:layout_marginBottom="8dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Commenter Avatar -->
        <FrameLayout
            android:id="@+id/commentAvatarContainer"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/circle_bg"
            android:clipToOutline="true"
            android:outlineProvider="background"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <ImageView
                android:id="@+id/ivCommentAvatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/ic_profile_placeholder"
                android:scaleType="centerCrop"
                app:tint="@color/colorPrimary"
                android:contentDescription="Commenter Avatar" />
        </FrameLayout>

        <!-- Username + Time -->
        <TextView
            android:id="@+id/tvCommenterName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Sayed Haneef"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="?attr/colorOnSurface"
            android:layout_marginStart="10dp"
            app:layout_constraintTop_toTopOf="@id/commentAvatarContainer"
            app:layout_constraintStart_toEndOf="@id/commentAvatarContainer"
            app:layout_constraintEnd_toStartOf="@id/tvCommentTime"
            app:layout_constraintBottom_toTopOf="@id/tvCommentText"
            android:ellipsize="end"
            android:maxLines="1" />

        <TextView
            android:id="@+id/tvCommentTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="2h ago"
            android:textSize="11sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            app:layout_constraintTop_toTopOf="@id/tvCommenterName"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/tvCommenterName" />

        <!-- Comment Text -->
        <TextView
            android:id="@+id/tvCommentText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="This is a really helpful post, thanks for sharing!"
            android:textSize="14sp"
            android:textColor="?attr/colorOnSurface"
            android:lineSpacingMultiplier="1.2"
            android:layout_marginTop="4dp"
            app:layout_constraintTop_toBottomOf="@id/tvCommenterName"
            app:layout_constraintStart_toStartOf="@id/tvCommenterName"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Comment Actions Row -->
        <LinearLayout
            android:id="@+id/commentActions"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/tvCommentText"
            app:layout_constraintStart_toStartOf="@id/tvCommenterName"
            app:layout_constraintEnd_toEndOf="parent">

            <LinearLayout
                android:id="@+id/btnCommentLike"
                android:layout_width="wrap_content"
                android:layout_height="28dp"
                android:orientation="horizontal"
                android:gravity="center"
                android:background="?attr/selectableItemBackground"
                android:paddingHorizontal="8dp">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/thumbsup"
                    app:tint="?attr/colorOnSurfaceVariant"
                    android:contentDescription="Like Comment" />

                <TextView
                    android:id="@+id/tvCommentLikeCount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="12"
                    android:textSize="12sp"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:layout_marginStart="4dp" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/btnCommentReply"
                android:layout_width="wrap_content"
                android:layout_height="28dp"
                android:orientation="horizontal"
                android:gravity="center"
                android:background="?attr/selectableItemBackground"
                android:paddingHorizontal="8dp"
                android:layout_marginStart="12dp">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_message"
                    app:tint="?attr/colorOnSurfaceVariant"
                    android:contentDescription="Reply" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Reply"
                    android:textSize="12sp"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:layout_marginStart="4dp" />
            </LinearLayout>
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
